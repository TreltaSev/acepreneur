export { default as _, default as Root } from './components/button.svelte';

export type { tButtonProps as Props } from './types';

export { buttonColors as Colors, buttonSizes as Sizes } from './types';
